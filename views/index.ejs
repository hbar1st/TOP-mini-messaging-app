<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/index.css">
    <title>Mini Messaging App</title>
</head>
<nav>
    <ul>
        <li><a href="http://localhost:3000/new">Add a new message</a></li>
    </ul>
</nav>
<body>
    <dl>
        <% messages.forEach((el,i) => { %>
            <div>
                <dt>User</dt>
                <dd><%= el.user %></dd>
                <dt>Text</dt>
                <dd><%= el.text %></dd>
                <dt>Date</dt>
                <dd><%= el.added %></dd>
            </div>
            <form method="GET" action="/details?index=<%= i %>"><button type="submit">View Details</button></form>
            <% if (i < messages.length-1)  { %>
                <hr>
            <% } %>
        <% }); %>
    </dl>
</body>
</html>